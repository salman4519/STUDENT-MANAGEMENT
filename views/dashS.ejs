<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }
        .container {
            background-color: #4CAF50;
            padding: 20px;
            color: white;
            text-align: center;
        }
        .dashboard-content {
            padding: 30px;
            text-align: center;
            background-color: #fff;
            margin: 20px auto;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 600px;
        }
        h1 {
            font-size: 36px;
            margin-bottom: 20px;
        }
        .user-info {
            font-size: 18px;
            margin-bottom: 30px;
            color: #333;
        }
        .logout-btn {
            padding: 10px 20px;
            background-color: #FF5722;
            color: white;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            cursor: pointer;
        }
        .logout-btn:hover {
            background-color: #E64A19;
        }
    </style>
</head>
<body>

<div class="container">
    <h1>Welcome to your Dashboard, <%= user.username %>!</h1>
</div>

<div class="dashboard-content">
    <p class="user-info"><strong>Name:</strong> <%= user.username %></p>
    <p class="user-info"><strong>Place:</strong> <%= user.place %></p>
    <p class="user-info"><strong>Mobile:</strong> <%= user.mobile %></p>

    <!-- Logout Button -->
    <form action="/logout" method="POST">
        <button type="submit" class="logout-btn">Logout</button>
    </form>
</div>

</body>
</html>
